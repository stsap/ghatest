
name: 
on:
  pull_request:
    branches:
      - main
    types: [closed]
    
jobs:
  tag:
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - name: pwd
        run: |
          echo ${github.event.pull_request}
    
